# Notify

This is not a public endpoint: this is a running cron job every morning at 8am to notify users whose invoices are due in 5 days.

```bash filename="Endpoints" copy
POST /api/invoice/notify
```
